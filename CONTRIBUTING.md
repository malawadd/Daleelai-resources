# دليل المساهمة في Daleelai Resources

هذا الملف هو المرجع العملي لإضافة الموارد وتعديلها داخل المستودع.

إذا كنت تريد فقط فهم فكرة المشروع أو بنية المستودع، ابدأ من [README.md](README.md). أما إذا كنت ستكتب أو تعدّل موردًا بالفعل، فتابع من هنا.

## ما الهدف من هذا المستودع؟

هذا المستودع هو مصدر المحتوى الذي يدخل إلى دليل AI. كل مورد يُكتب هنا قد يتحول إلى مسودة داخل النظام، ثم يُراجع ويُنشر لاحقًا.

بمعنى عملي:

1. نضيف أو نعدّل موردًا داخل GitHub.
2. يقرأ النظام ملفات `resources/`.
3. تظهر الموارد داخل لوحة الإدارة كمحتوى قابل للمراجعة.
4. يراجعها الفريق ثم ينشر المناسب منها.

## ما الذي يمكن إضافته؟

نرحب بموارد مثل:

- أدوات ومنصات AI
- مشاريع agents ومشاريع مفتوحة المصدر
- شروحات وأدلة عملية
- مجتمعات عربية أو إقليمية
- مدونات ونشرات
- قنوات فيديو أو بودكاست
- مجموعات بيانات
- برومبتات
- أبحاث وتقارير

## معايير القبول التحريرية

المورد الجيد هنا عادة يكون:

- واضح المصدر
- مفيدًا عمليًا
- قابلًا للوصف بموضوعية
- مناسبًا للجمهور العربي أو ذا قيمة واضحة له

والمورد الضعيف عادة يكون:

- مجرد إعلان تسويقي
- صفحة فقيرة لا تقدم مادة فعلية
- نسخة مكررة من مورد موجود
- رابطًا غير واضح أو غير مستقر

## اختر التصنيف الصحيح

اختر تصنيفًا رئيسيًا واحدًا فقط:

- `general`
- `agents`
- `frameworks`
- `tools-platforms`
- `tutorials-guides`
- `blogs`
- `communities`
- `newsletters`
- `datasets`
- `prompts`
- `research-reports`
- `videos-podcasts`

إرشاد سريع:

- استخدم `agents` إذا كان المورد نفسه agent أو مشروعًا agentic واضحًا.
- استخدم `tools-platforms` إذا كان المورد أداة أو منصة عامة.
- استخدم `general` إذا كان المورد مفيدًا لكن لا يقع بوضوح داخل تصنيف تخصصي أدق.
- استخدم `videos-podcasts` عادة للمحتوى السمعي/المرئي الفردي أو القنوات إذا أردت تصنيفها من زاوية المحتوى، لكن إن كان الفريق يعتمد حاليًا `general` لبعض القنوات أو الموارد الجامعة فاتبع النمط الموجود داخل المستودع.

## مكان الملف وصيغته

الصيغة الموصى بها:

```text
resources/<category>/<slug>/resource.md
```

مثال:

```text
resources/tools-platforms/example-tool/resource.md
```

توجد صيغ أخرى يفهمها النظام:

- `resources/<category>/<slug>.md`
- `resources/<slug>.md`

لكن لا تستخدمها إلا إذا كان هناك سبب واضح.

## قواعد تسمية الـ slug

يفضّل أن يكون:

- بالإنجليزية أو transliteration بسيطة
- بحروف صغيرة فقط
- مفصولًا بشرطات `-`
- قصيرًا وواضحًا

مثال جيد:

```text
tokenai-horus
```

مثال غير جيد:

```text
My Amazing Arabic Resource Final Version 2
```

## ابدأ من قالب قريب

إذا كنت تضيف موردًا جديدًا، ابدأ بقالب قريب من نوع المورد داخل:

```text
templates/resources/
```

أمثلة مفيدة:

- [templates/resources/blogs/arabic-agent-notes/resource.md](templates/resources/blogs/arabic-agent-notes/resource.md)
- [templates/resources/communities/arabic-ai-builders/resource.md](templates/resources/communities/arabic-ai-builders/resource.md)
- [templates/resources/tools-platforms/serpapi-for-agents/resource.md](templates/resources/tools-platforms/serpapi-for-agents/resource.md)
- [templates/resources/tutorials-guides/arabic-rag-blueprint/resource.md](templates/resources/tutorials-guides/arabic-rag-blueprint/resource.md)

## القالب المعتمد

```md
---
title: "اسم المورد"
description: "وصف مختصر وواضح يشرح لماذا هذا المورد مهم."
category: "tools-platforms"
tags:
  - "عربي"
  - "أداة"
  - "مرجع"
contentType: "reference"
language: "العربية"
pricing: "مجاني"
platform: "GitHub"
sourceLabel: "المصدر الرسمي"
sourceUrl: "https://example.com"
featured: false
createdAt: "2026-05-08"
whyItMattersTitle: "عنوان قصير يجيب مباشرة: لماذا يهم هذا المورد؟"
whyItMattersPoints:
  - title: "سبب أول"
    description: "جملة قصيرة تبرر العنوان وتشرح قيمة عملية واضحة."
  - title: "سبب ثان"
    description: "جملة قصيرة تضيف زاوية ثانية من القيمة."
  - title: "سبب ثالث"
    description: "جملة قصيرة تشرح لماذا قد يعود المستخدم لهذا المورد."
---

## لماذا نضيفه إلى الدليل؟

اشرح القيمة الفعلية للمورد بوضوح واختصار.

## ما الذي يميّزه؟

- نقطة أولى
- نقطة ثانية
- نقطة ثالثة
```

## شرح الحقول المهمة

- `title`: اسم المورد كما سيظهر للمستخدم.
- `description`: وصف مختصر يظهر في البطاقة وصفحة المورد.
- `category`: تصنيف رئيسي واحد فقط.
- `tags`: وسوم تساعد على الفهم والبحث السريع.
- `contentType`: واحدة من `guide` أو `toolkit` أو `reference` أو `dataset` أو `community`.
- `language`: لغة المحتوى نفسه، مثل `العربية` أو `العربية + الإنجليزية`.
- `pricing`: مثل `مجاني` أو `مجاني + مدفوع`.
- `platform`: نوع المنصة أو مكان توفر المورد.
- `sourceLabel`: الاسم المختصر الذي يظهر بجانب رابط المصدر.
- `sourceUrl`: الرابط الأصلي للمورد.
- `featured`: اتركها `false` افتراضيًا.
- `createdAt`: تاريخ الإضافة بصيغة `YYYY-MM-DD`.
- `whyItMattersTitle`: عنوان قصير ومباشر يجيب عن سؤال "لماذا يهم؟".
- `whyItMattersPoints`: ثلاث نقاط قصيرة تبرر هذا العنوان وتفصّل قيمته.

## قواعد مهمة أثناء الكتابة

- اكتب أسماء الحقول نفسها بالإنجليزية كما هي.
- يمكن أن تكون قيم الحقول بالعربية إذا كان ذلك أنسب.
- اجعل المحتوى النهائي بالعربية الواضحة والمباشرة.
- لا تكتب وصفًا دعائيًا مبالغًا فيه.
- إذا لم تستطع التحقق من معلومة، لا تقدّمها كحقيقة مؤكدة.
- إذا كان لديك رأي تحريري، اجعله متزنًا ومسنودًا بما يظهر من المورد نفسه.

## كيف نصوغ `whyItMatters` بشكل صحيح؟

هذا الجزء مهم جدًا لأن التطبيق يعرضه كصندوق مستقل.

استخدمه بهذه الطريقة:

- `whyItMattersTitle`: الخلاصة القصيرة. يجب أن تقرأ كإجابة مباشرة على سؤال "لماذا يهم هذا المورد؟"
- `whyItMattersPoints`: الأسباب الثلاثة التي تبرر هذه الخلاصة

مثال جيد:

- العنوان: `مسار عربي متدرج بين الأساسيات والتطبيق`
- النقاط: تشرح لماذا هو متدرج، ولماذا هو مفيد، وما الذي يجعله مناسبًا للمتابعة

مثال غير جيد:

- العنوان: `لماذا قد تتابع هذه القناة؟`

هذا ليس جوابًا، بل إعادة صياغة للسؤال نفسه.

## ماذا نكتب داخل جسم المورد؟

يفضّل أن يجيب المحتوى على أسئلة مثل:

- لماذا يستحق هذا المورد الإضافة؟
- لمن يناسب؟
- ما الذي سيستفيده المستخدم منه؟
- هل توجد ملاحظات أو حدود أو تحفظات مهمة؟

الأسلوب الأفضل عادة:

- واضح
- مختصر نسبيًا
- تفسيري
- غير دعائي

## كتل Markdoc المدعومة

يمكن استخدام:

- `callout`
- `accordion`
- `tabs` و `tab`
- `video`
- `slides`

مثال:

```md
{% callout title="ملاحظة مهمة" tone="tip" %}
هذا المورد مناسب أكثر لمن لديه أساس أولي في المجال.
{% /callout %}
```

ومثال فيديو:

```md
{% video
  title="مثال من المحتوى"
  src="https://www.youtube.com/watch?v=SSo_EIwHSd4"
  caption="فيديو تمثيلي يعطي فكرة واضحة عن نوع المحتوى داخل هذا المورد."
/%}
```

## قبل فتح Pull Request

راجع هذه القائمة:

- هل الملف موجود داخل `resources/`؟
- هل اخترت التصنيف الأنسب؟
- هل الـ slug واضح وقصير؟
- هل `sourceUrl` صحيح ويعمل؟
- هل `description` واضحة ومختصرة؟
- هل `tags` مفيدة فعلًا؟
- هل `whyItMattersTitle` يجيب عن السؤال مباشرة؟
- هل النقاط الثلاث تبرر العنوان؟
- هل المحتوى عربي وواضح وغير دعائي؟
- هل تجنبت التكرار مع مورد موجود سابقًا؟

## كيف نراجع المساهمات؟

عادة ننظر إلى:

- جودة المورد نفسه
- وضوح الكتابة
- صحة التصنيف
- صحة المصدر
- وجود قيمة فعلية للمستخدم
- التوازن بين الحماس والتحفظ التحريري

قد نطلب تعديلات على:

- التصنيف
- الوسوم
- صياغة الوصف
- عنوان المورد
- جزء `whyItMatters`

## ملفات مرتبطة

- [README.md](README.md)
